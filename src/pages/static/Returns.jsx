import React from 'react';
import { Navigate } from 'react-router-dom';

const Returns = () => {
    return <Navigate to="/helpcentre?catalog=Returns%20%26%20Refunds" replace />;
};

export default Returns;
